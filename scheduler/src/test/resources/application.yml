spring:
  r2dbc:
    url: r2dbc:h2:mem:///testdb
    username: sa
    password:
    pool:
      initial-size: 2
      max-size: 5

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

flowerdata:
  api:
    base-url: http://localhost:8888
    service-key: my-service-key
